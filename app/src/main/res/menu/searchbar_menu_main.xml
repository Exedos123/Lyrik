<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <group android:id="@+id/Selct_items">
        <item
            android:id="@+id/switch_action_bar"
            android:title="lang"
            app:actionLayout="@layout/use_switch"
            app:showAsAction="always"
            />



    <item android:id="@+id/searchBtn"
        android:icon="@drawable/search_btn_app_bar"
        android:title="search"
        app:showAsAction="always|collapseActionView"
        app:actionViewClass="androidx.appcompat.widget.SearchView"/>
    <item android:id="@+id/AccountBtn"
        android:title="MyAccountPage"
        android:icon="@drawable/account_black"
        app:showAsAction="always"
      />

    <item android:id="@+id/uploadBtn"
        android:title="Uploading a Song"
        android:icon="@drawable/song_add"
        app:showAsAction="always"
     />
    </group>



</menu>